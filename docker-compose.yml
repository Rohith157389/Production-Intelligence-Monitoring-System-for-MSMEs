services:
  postgres:
    image: postgres:16-alpine
    container_name: pmrs-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pmrs
    ports:
      - "5432:5432"
    volumes:
      - pmrs_data:/var/lib/postgresql/data

volumes:
  pmrs_data:
